#include <stdbool.h>

float add(float, float);
float multiply(float, float);
float divide(float, float);
float subtract(float, float);
float modulus(float, float);
bool isNum(char*);
float handleCal(char*, char*, char*);
int main(int, char**);
